<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/main_menu_icon"
        android:icon="@drawable/ic_database"
        android:title="@string/main_menu_icon"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/create_chat_icon"
        android:icon="@drawable/ic_comment_processing"
        android:title="@string/create_chat_icon"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />
</menu>
